# Generated by MemoryPack's TypeScript source generator — kept in the
# generator's own style, not prettier's. Excluded so `npm run format` doesn't
# churn thousands of lines that regeneration would immediately revert.
src/network/protocol/

# Build output & dependencies
dist/
node_modules/
